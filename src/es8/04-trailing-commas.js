'use strict';

//Trailing commas

const arr = [1, 2, 3, 4, 5, 6, 7, 8, , , ,];
/* Las trailing commas son una especie de spaceholder para arrays que
    nos permite jugar un poco con el largo de un array mientras esperamos 
    a añadir los valores finales que irían en esos placeholders */
console.log(arr);
console.log(arr.length);